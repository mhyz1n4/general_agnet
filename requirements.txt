# Core LLM SDKs
openai>=1.0.0
anthropic>=0.30.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Agent framework
strands-agents[anthropic]>=0.1.0

# Memory backend (ReMeLight + its agentscope dependency)
reme-ai>=0.3.1
agentscope>=1.0.0

# Cross-process file locking (JSONIndexer)
filelock>=3.0.0

# Config & settings
pydantic-settings>=2.0.0

# YAML parsing (eval harness fixtures)
PyYAML>=6.0

# Templating
jinja2>=3.1.0

# Terminal UI
rich>=13.0.0

# Optional: For testing and development
pytest>=8.0.0
black>=24.0.0
mypy>=1.9.0
