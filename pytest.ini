[pytest]
markers =
    integration: tests that require external infrastructure (Redis, real filesystem)
    benchmark: latency benchmarks measuring P50/P95 targets
    llm: tests that require a live LLM endpoint (vLLM or OpenAI-compatible)
    eval: end-to-end golden-fixture tests against a live LLM
